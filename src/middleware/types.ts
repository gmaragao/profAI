export type ClassifiedIntentFromAgent = {
  userId: string;
  courseId: string;
  summarizedInput: string;
  forumId: string;
  postId: string;
  intent: string;
  source: string;
  createdAt: string;
  updatedAt: string;
};
